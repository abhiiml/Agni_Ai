[project]
name = "agni-ai"
version = "1.0.0"
description = "Automated Geospatial Network for Industrial Heat Detection (SRS-FIRMS-INDUSTRY-001)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "pydantic>=2.0.0",
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "main:app"
